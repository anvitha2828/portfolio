# Images

Drop your images here and reference them from code as `/images/your-file.jpg`.

Suggested files:
- `headshot.jpg` — used on the About page (see `app/about/page.tsx`)
- Case-study covers — set the `cover` field in `content/caseStudies.ts`,
  e.g. `cover: "/images/project-a.jpg"`
